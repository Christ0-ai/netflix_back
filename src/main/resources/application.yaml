spring:
  application:
    name: netflix
  datasource:
    url: ${DATABASE_URL}
    password: ${DATABASE_PASSWORD}
    username: ${DATABASE_USERNAME}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

